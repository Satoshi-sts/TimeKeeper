<Window x:Class="NotificationTabApp.Controls.NotificationPopup"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Notification"
        Width="260"
        SizeToContent="Height"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        ShowActivated="False">
    <Border x:Name="PopupBorder"
            Background="#A8222222"
            BorderBrush="#80FFFFFF"
            BorderThickness="1"
            CornerRadius="6"
            Padding="10,8"
            MouseEnter="PopupBorder_MouseEnter"
            MouseLeave="PopupBorder_MouseLeave">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="TitleText"
                         Grid.Column="0"
                         Foreground="White"
                         FontWeight="Bold"
                         FontSize="12"
                         TextWrapping="Wrap"
                         VerticalAlignment="Center"
                         IsReadOnly="True"
                         BorderThickness="0"
                         Background="Transparent"
                         Padding="0"
                         Cursor="IBeam"/>
                <Button Grid.Column="1"
                        Content="🔕"
                        Width="20" Height="20"
                        Background="Transparent"
                        BorderBrush="Transparent"
                        Foreground="#DDDDDD"
                        FontSize="11"
                        Padding="0"
                        Cursor="Hand"
                        ToolTip="この通知をミュート"
                        Click="MuteButton_Click"
                        VerticalAlignment="Top"/>
                <Button Grid.Column="2"
                        Content="↗"
                        Width="20" Height="20"
                        Background="Transparent"
                        BorderBrush="Transparent"
                        Foreground="#DDDDDD"
                        FontSize="11"
                        Padding="0"
                        Cursor="Hand"
                        ToolTip="この通知を開く"
                        Click="OpenButton_Click"
                        VerticalAlignment="Top"/>
                <Button Grid.Column="3"
                        Content="x"
                        Width="20" Height="20"
                        Background="Transparent"
                        BorderBrush="Transparent"
                        Foreground="#AAAAAA"
                        FontSize="11"
                        Padding="0"
                        Cursor="Hand"
                        Click="CloseButton_Click"
                        VerticalAlignment="Top"/>
            </Grid>
            <TextBox x:Name="TimeText"
                     Grid.Row="1"
                     Foreground="#BBBBBB"
                     FontSize="10"
                     TextWrapping="Wrap"
                     Margin="0,2,0,0"
                     IsReadOnly="True"
                     BorderThickness="0"
                     Background="Transparent"
                     Padding="0"
                     Cursor="IBeam"/>
            <TextBox x:Name="MessageText"
                     Grid.Row="2"
                     Foreground="#DDDDDD"
                     FontSize="11"
                     TextWrapping="Wrap"
                     Margin="0,4,0,0"
                     IsReadOnly="True"
                     BorderThickness="0"
                     Background="Transparent"
                     Padding="0"
                     AcceptsReturn="True"
                     Cursor="IBeam"/>
        </Grid>
    </Border>
</Window>
