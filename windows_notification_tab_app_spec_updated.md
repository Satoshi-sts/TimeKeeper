# Windows 通知タブアプリ 仕様書

作成日: 2026-05-17  
更新日: 2026-05-18  
対象アプリ: TimeKeeper

---

## 1. アプリ概要

TimeKeeper は、Windows デスクトップ上に小さなタブ状ウィンドウを常駐表示し、登録した時刻になったら通知ポップアップと通知音で知らせるデスクトップアプリである。

通知時刻は、現実時間または FF14 のエオルゼア時間（ET）で設定できる。通知は通常画面付近に薄く半透明で表示し、他アプリやゲームの操作フォーカスを奪わない。

主な機能は以下とする。

- 小さな半透明タブの常駐表示
- 常に最前面表示の ON/OFF
- 通知項目の登録、編集、削除
- 通知グループ管理
- グループ単位の通知 ON/OFF
- 現実時間通知
- エオルゼア時間通知
- 一度限りの予定
- 直近予定一覧
- 直近予定のスキップ
- アプリ内からの更新確認
- アプリ内からのワンボタン更新

---

## 2. 画面構成

| 画面 | 役割 |
|---|---|
| 通常画面 | 常駐タブ、ピン切替、直近予定一覧起動、通知設定画面起動、アプリ更新画面起動 |
| 設定画面 | 通知項目または一度限り予定の新規作成、既存通知項目の編集 |
| 登録通知一覧画面 | 登録済み通知の一覧、グループ折りたたみ、通知 ON/OFF、削除 |
| 直近予定一覧画面 | 直近10件の予定表示、予定スキップ |
| アプリ更新画面 | 現在バージョン表示、更新確認、更新内容表示、アプリ更新実行 |
| 通知ポップアップ | 通知発火時の小型表示 |

---

## 3. 通常画面仕様

通常画面は小さなタブ状ウィンドウとして表示する。

- 起動時は画面右上付近に表示する
- ドラッグで移動できる
- 半透明で表示する
- マウスオーバー時は通常時より濃く表示する
- 終了時に位置とピン状態を保存する
- 次回起動時に位置とピン状態を復元する

表示要素は以下の順序とする。

```text
[TimeKeeper] [ピン] [直近予定一覧] [プラス] [歯車]
```

| 要素 | 説明 |
|---|---|
| TimeKeeper | アプリ名 |
| ピン | 常に最前面表示の ON/OFF |
| 直近予定一覧 | 直近予定一覧画面を開く |
| プラス | 設定画面を開く。通知項目または一度限り予定を登録する |
| 歯車 | アプリ更新画面を開く |

従来の歯車ボタンで行っていた「設定画面を開く」処理は、プラスボタンへ移植する。  
歯車ボタンは、通知設定ではなくアプリ更新画面を開く操作として扱う。

### 3.1 ピン

- ピン ON の場合、通常画面は常に最前面で表示する
- ピン OFF の場合、通常ウィンドウと同じ重なり順で扱う
- 初期状態はピン ON とする

### 3.2 最小化

- 最小化時は通常画面を非表示にする
- 最小化中も通知監視は継続する
- 通知発火時に操作中の他ウィンドウへ悪影響を与えないため、通知発火を理由に通常画面をアクティブ化しない

### 3.3 プラスボタン

プラスボタンは、通知項目または一度限り予定を登録するための設定画面を開く。

- クリック時、設定画面を別ウィンドウで開く
- 既に設定画面が開いている場合は、既存の設定画面を前面に表示する
- プラスボタンは、通知追加・予定追加を連想できる見た目にする
- 歯車ボタンとは用途を分離する

### 3.4 歯車ボタン

歯車ボタンは、アプリ更新画面を開く。

- クリック時、アプリ更新画面を別ウィンドウで開く
- 既にアプリ更新画面が開いている場合は、既存のアプリ更新画面を前面に表示する
- 歯車ボタンでは通知設定画面を開かない

---

## 4. 通知ポップアップ仕様

通知発火時は通常画面付近に小さな通知ポップアップを表示する。

- 半透明で薄く表示する
- マウスオーバー時は少し濃く表示する
- タイトル、時間、本文、閉じるボタンを表示する
- 通知音を鳴らす
- 他アプリやゲームのフォーカスを奪わない
- タスクバーには表示しない
- 複数通知が同時に出る場合は、通常画面の下に登録順で縦に並べる

表示内容:

```text
[タイトル]                         [x]
現実時間 09:00 - 09:01
本文
```

ET 通知の場合:

```text
[タイトル]                         [x]
ET 08:00 - 09:00
本文
```

閉じるボタンを押した通知は、同じ発火単位内では再表示しない。

---

## 5. 通知項目仕様

通知項目は以下を持つ。

| 項目 | 説明 |
|---|---|
| タイトル | 通知タイトル |
| グループ | 所属グループ。未指定の場合は無所属 |
| 一度限り | ON の場合、登録通知一覧には表示しない単発予定として扱う |
| エオルゼア時間 | ON の場合、開始/終了時刻を ET として扱う |
| 通知開始時間 | HH:mm 形式 |
| 通知終了時間 | HH:mm 形式 |
| 表示文字 | 通知本文 |
| 通知対象 | 通知対象かどうか |

通常通知は登録通知一覧に表示する。  
一度限り予定は登録通知一覧には表示せず、直近予定一覧と通知判定の対象にする。

---

## 6. 時間軸仕様

通知項目ごとに時間軸を選択できる。

| timeMode | 意味 | 判定に使う現在時刻 | 繰り返し単位 |
|---|---|---|---|
| `real` | 現実時間 | PC の現在時刻 | 現実日付ごと |
| `eorzea` | エオルゼア時間 | PC 時刻から算出した ET | ET 日番号ごと |

エオルゼア時間 ON の場合、入力された HH:mm は ET の HH:mm として扱う。  
現実時間を ET に自動変換して保存することはしない。

### 6.1 ET 計算

ET は PC の UTC 時刻から算出する。FF14 の画面、ログ、ゲームプロセスは読み取らない。

| 項目 | 値 |
|---|---:|
| ET 1日 | 現実時間70分 |
| ET 進行倍率 | `1440 / 70` |

計算方針:

```text
ET進行倍率 = 1440 / 70
現在Unix秒 = 現在UTC時刻のUnix秒
ET合計秒 = 現在Unix秒 * ET進行倍率
ET日番号 = floor(ET合計秒 / 86400)
ET日内秒 = floor(ET合計秒) % 86400
ET時刻 = ET日内秒から HH:mm:ss を算出
```

---

## 7. 通知判定仕様

通知監視は一定間隔で実行する。現行実装では10秒間隔とする。

通知対象になる条件:

- 通知項目が有効である
- 所属グループが有効である、または無所属である
- 現在時刻が通知開始時間から通知終了時間の範囲内である
- 同じ発火単位でまだ通知していない
- 直近予定一覧で該当発火単位をスキップしていない

日跨ぎ範囲も許可する。

| 開始 | 終了 | 現在 | 判定 |
|---|---|---|---|
| 09:00 | 09:01 | 09:00 | 範囲内 |
| 23:50 | 00:10 | 23:55 | 範囲内 |
| 23:50 | 00:10 | 00:05 | 範囲内 |
| 23:50 | 00:10 | 00:20 | 範囲外 |

---

## 8. 一度限り予定仕様

設定画面で「一度限りの予定」を ON にして保存すると、一度限り予定として作成する。

- デフォルトは OFF
- 登録通知一覧には表示しない
- 直近予定一覧には表示する
- 通知判定の対象になる
- 通知が発火したら発火済みにする
- 発火後は直近予定一覧から除外する
- 通知終了後、保存データから削除する
- 直近予定一覧でスキップした場合も削除する

編集画面では既存の通常通知を一度限り予定へ変更しない。  
一度限り予定は登録通知一覧に表示されないため、編集対象外とする。

---

## 9. グループ仕様

通知項目はグループに所属できる。

- グループ未指定の場合は無所属とする
- 設定画面で既存グループを選択できる
- 設定画面で新規グループを追加できる
- 登録通知一覧画面ではグループごとに折りたたみ表示する
- グループごとに通知 ON/OFF を切り替えられる
- グループ OFF の場合、配下の各通知項目が ON でも通知しない
- グループ OFF は上位条件であり、配下通知項目の個別 ON/OFF 状態は変更しない
- 無所属にはグループ ON/OFF を表示しない

---

## 10. 設定画面仕様

本仕様書における設定画面は、通知項目または一度限り予定を登録・編集する画面を指す。  
通常画面のプラスボタンから開く。歯車ボタンからは開かない。

設定画面では以下を入力できる。

| 項目 | 入力形式 | 必須 | 説明 |
|---|---|---:|---|
| タイトル | テキストボックス | 必須 | 通知タイトル |
| グループ | プルダウン | 任意 | 無所属または登録済みグループ |
| 新規グループ名 | テキストボックス | 任意 | 追加ボタンでグループ追加 |
| 一度限りの予定 | チェックボックス | 任意 | ON の場合、単発予定として保存 |
| エオルゼア時間 | チェックボックス | 任意 | ON の場合、時刻を ET として扱う |
| 通知開始時間 | 1分刻みプルダウン | 必須 | HH:mm |
| 通知終了時間 | 1分刻みプルダウン | 必須 | HH:mm |
| 表示文字 | 複数行テキストボックス | 必須 | 通知本文 |

開始時間を選択すると、終了時間に開始時間の1分後を自動入力する。  
開始時間を変更した場合、終了時間は再度1分後へ更新する。

---

## 11. 登録通知一覧画面仕様

登録通知一覧画面は、通常通知のみを表示する。  
一度限り予定は表示しない。

一覧はグループ単位で表示し、各グループは折りたたみできる。

通常モードの表示:

```text
[グループ通知チェック] グループ名                     件数
  [通知チェック] 通知 タイトル
                 現実時間 09:00 - 09:01
                 本文プレビュー
```

操作:

- 通知チェックで個別通知の ON/OFF を切り替える
- グループチェックでグループ単位の ON/OFF を切り替える
- 項目本文領域クリックで編集画面を開く
- 削除ボタンで削除モードに入る
- 削除モード中はキャンセルボタンで通常モードに戻る

削除モードでは削除対象チェックを表示する。  
削除対象チェックは通知対象チェックと区別できる赤系表示とする。

---

## 12. 直近予定一覧画面仕様

通常画面の直近予定一覧ボタンを押すと表示する。

表示対象:

- 通常通知の次回発火予定
- 一度限り予定の次回発火予定
- 有効な通知のみ
- 所属グループが有効な通知のみ
- スキップ済み発火単位は除外

表示件数は直近から10件までとする。

表示内容:

```text
タイトル                         [x]
現実時間 09:00 - 09:01
```

ET 通知の場合:

```text
タイトル                         [x]
ET 08:00 - 09:00
```

### 12.1 スキップ

各項目の `x` ボタンでスキップできる。

スキップ時は確認ダイアログを表示する。

通常通知の場合:

- 該当する次回発火単位のみスキップする
- 以降の繰り返し通知は通常通り通知する

一度限り予定の場合:

- スキップすると削除する
- 以後通知されない

---

## 13. アプリ更新画面・更新機能仕様

通常画面の歯車ボタンを押すと、アプリ更新画面を表示する。  
アプリ更新画面では、現在のバージョン表示、更新確認、更新内容確認、更新実行を行う。

### 13.1 表示項目

アプリ更新画面には以下を表示する。

| 項目 | 説明 |
|---|---|
| 現在のバージョン | 実行中アプリのバージョン |
| 更新を確認ボタン | 最新版が存在するか確認する |
| 最新バージョン | 更新可能な場合に表示する |
| 更新内容 | 更新可能な場合に表示する。リリースノートまたは変更内容 |
| 更新するボタン | 更新可能な場合に有効化する |

初期表示では、現在のバージョンと更新を確認ボタンを表示する。  
最新バージョン、更新内容、更新するボタンは、更新可能な場合に表示または有効化する。

表示例:

```text
現在のバージョン: 1.0.0
[更新を確認]

新しいバージョンがあります。
最新バージョン: 1.1.0

更新内容:
- 通知表示の安定性を改善
- エオルゼア時間通知の判定を修正

[更新する]
```

最新版が存在しない場合:

```text
現在のバージョン: 1.0.0
[更新を確認]

現在最新版です。
```

### 13.2 更新方式

更新機能は、ユーザーがアプリ内からワンボタンで最新版へ更新できる方式とする。

- ユーザーにダウンロードページを開かせる方式にはしない
- アプリが直接、最新版情報と更新パッケージを取得する
- 更新処理には Velopack を利用する想定とする
- 更新パッケージの配置先は GitHub Releases または静的ファイル配信先を想定する
- 更新確認、ダウンロード、適用、再起動までをアプリ側の操作で完結させる

### 13.3 更新確認

「更新を確認」ボタン押下時、アプリは配布先に公開されている最新版情報を取得し、現在のアプリバージョンと比較する。

更新がない場合:

- 「現在最新版です。」と表示する
- 更新するボタンは表示しない、または無効化する

更新がある場合:

- 「新しいバージョンがあります。」と表示する
- 最新バージョンを表示する
- 更新内容を表示する
- 更新するボタンを有効化する

更新確認中は、ボタンの多重押下を防止する。

### 13.4 更新実行

「更新する」ボタン押下時、アプリは更新パッケージをダウンロードする。

- ダウンロード中は進捗を表示する
- ダウンロード中は更新するボタンを無効化する
- ダウンロード完了後、更新適用確認を表示する

確認表示:

```text
更新を適用するにはアプリを再起動します。
今すぐ再起動しますか？

[今すぐ再起動] [あとで]
```

「今すぐ再起動」を押した場合:

- アプリを終了する
- 更新を適用する
- 更新後にアプリを再起動する

「あとで」を押した場合:

- 更新画面を閉じてもよい
- 次回起動時または任意のタイミングで更新適用できる実装が可能な場合は適用待ち状態を保持する
- 実装が複雑になる場合、初期実装では「あとで」を押した時点で更新適用をキャンセルしてもよい

### 13.5 更新エラー時の挙動

更新確認または更新ダウンロードに失敗した場合は、ユーザーに分かる形でエラーを表示する。

表示例:

```text
更新確認に失敗しました。
インターネット接続を確認して、しばらくしてから再度お試しください。
```

エラー時の条件:

- アプリを異常終了させない
- 既存バージョンの起動状態を維持する
- 更新失敗によってアプリが起動不能になる状態を避ける
- 失敗時は再度「更新を確認」または「更新する」を押せるようにする

### 13.6 バージョン取得

現在のバージョンは、アプリ本体のバージョン情報から取得する。  
設定ファイルに現在バージョンを手動保存して管理しない。

---

## 14. データ仕様

保存場所:

```text
%LOCALAPPDATA%\NotificationTabApp\settings.json
```

トップレベル:

| フィールド | 型 | 説明 |
|---|---|---|
| mainWindow | object | 通常画面設定 |
| notificationGroups | array | 通知グループ |
| notifications | array | 通常通知 |
| oneTimeNotifications | array | 一度限り予定 |
| skippedOccurrences | array | スキップ済み発火単位 |

### 14.1 通知項目

| フィールド | 型 | 説明 |
|---|---|---|
| id | string | 一意ID |
| title | string | タイトル |
| groupId | string | グループID。空文字は無所属 |
| timeMode | string | `real` または `eorzea` |
| startTime | string | HH:mm |
| endTime | string | HH:mm |
| message | string | 本文 |
| enabled | boolean | 個別通知ON/OFF |
| isOneTime | boolean | 一度限り予定か |
| hasFired | boolean | 一度限り予定が発火済みか |
| createdAt | string | ISO 8601 |
| updatedAt | string | ISO 8601 |

### 14.2 通知グループ

| フィールド | 型 | 説明 |
|---|---|---|
| id | string | 一意ID |
| name | string | グループ名 |
| enabled | boolean | グループ通知ON/OFF |
| createdAt | string | ISO 8601 |
| updatedAt | string | ISO 8601 |

### 14.3 スキップ済み発火単位

| フィールド | 型 | 説明 |
|---|---|---|
| notificationId | string | 通知ID |
| triggerKey | string | 発火単位キー |

発火単位キー例:

```text
real:2026-05-18
et:123456789
```

---

## 15. エラー処理

設定ファイルが存在しない場合:

- 初期設定で起動する
- 通常画面は画面右上付近
- ピンON
- 通知項目なし

設定ファイルが壊れている場合:

- 壊れた設定ファイルを `settings.json.bak` にバックアップする
- 初期設定で起動する
- ユーザーに読み込み失敗を通知する

通知音再生に失敗した場合:

- 通知表示は継続する
- アプリを異常終了させない

更新確認または更新ダウンロードに失敗した場合:

- 更新画面にエラー内容を表示する
- アプリを異常終了させない
- 既存バージョンの起動状態を維持する

---

## 16. 受け入れ条件

- アプリ起動時に通常画面が表示される
- 通常画面をドラッグ移動できる
- ピンON/OFFを切り替えられる
- 通常画面にプラスボタンが表示される
- プラスボタン押下で設定画面を開ける
- 通常画面に歯車ボタンが表示される
- 歯車ボタン押下でアプリ更新画面を開ける
- 設定画面から通常通知を登録できる
- 設定画面から一度限り予定を登録できる
- 設定画面からグループを追加できる
- 通知開始時間/終了時間は1分刻みプルダウンで選択できる
- エオルゼア時間ONの通知はETで判定される
- 通知発火時に通知音が鳴る
- 通知ポップアップは薄く半透明で表示される
- 通知ポップアップにタイトル、時間、本文が表示される
- 通知ポップアップは他アプリのフォーカスを奪わない
- 同時通知は重ならず縦に並ぶ
- 登録通知一覧はグループごとに折りたためる
- グループ単位で通知ON/OFFを切り替えられる
- グループOFFでも配下通知の個別ON/OFF状態は維持される
- 直近予定一覧に直近10件が表示される
- 直近予定一覧から次回通知をスキップできる
- 一度限り予定は発火後またはスキップ後に直近予定から消える
- アプリ更新画面に現在のバージョンが表示される
- アプリ更新画面から更新確認できる
- 更新可能な場合、最新バージョンと更新内容が表示される
- 更新可能な場合、更新するボタンからアプリ更新を開始できる
- 更新確認または更新実行に失敗しても、アプリが異常終了しない

---

## 17. 初期実装対象外

以下は現時点では対象外とする。

- 曜日指定通知
- 日付指定通知
- 通知音の種類選択
- 通知音の音量設定
- OS起動時の自動起動
- タスクトレイ常駐
- クラウド同期
- 複数端末同期
- ユーザーアカウント管理
- テーマ切替
- 詳細な通知履歴画面

