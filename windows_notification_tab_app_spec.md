# Windows通知タブアプリ 仕様書

作成日: 2026-05-17  
目的: Antigravity / Codex IDE などの実装エージェントに読み込ませ、Windows向けデスクトップアプリを実装するための仕様を定義する。  
更新内容: FF14のエオルゼア時間（ET）による通知設定・通知判定を追加。

---

## 1. アプリ概要

本アプリは、Windowsデスクトップ上に小さな「タブ状ウィンドウ」を常駐表示し、事前に登録した時刻になったら指定テキストを表示し、同時に通知音を鳴らすデスクトップ通知アプリである。

通知時刻は、現実時間だけでなく、FF14のゲーム内時間であるエオルゼア時間（ET）でも設定できる。エオルゼア時間で通知する場合、設定画面上の通知開始時間・通知終了時間は、現実時間ではなくエオルゼア時間として入力・保存・判定する。

通常時は画面右上付近に小さく表示され、半透明で目立ちすぎない見た目にする。設定画面から通知内容を登録・編集し、登録通知一覧画面から通知対象の有効/無効切替や削除を行える。

---

## 2. 想定プラットフォーム

- 対象OS: Windows
- アプリ種別: デスクトップアプリ
- 画面構成:
  - 通常画面
  - 設定画面
  - 設定画面 - 登録通知一覧画面
- 起動時に表示する画面: 通常画面

---

## 3. 技術選定について

実装技術は任意とする。  
ただし、以下の要件を満たせる技術を選定すること。

- Windowsデスクトップアプリとして起動できること
- ウィンドウの半透明描画ができること
- ウィンドウの常に最前面表示を制御できること
- 小さいタブ状ウィンドウをドラッグ移動できること
- ローカルに設定データを保存できること
- 指定時刻に通知表示と通知音再生ができること

候補例:

- WPF
- WinUI 3
- .NET MAUI Windows
- Electron
- Tauri

---

## 4. 用語定義

| 用語 | 説明 |
|---|---|
| 通常画面 | 起動時に表示される小さなタブ状ウィンドウ |
| 設定画面 | 通知項目を新規登録・編集する画面 |
| 登録通知一覧画面 | 登録済み通知を一覧表示し、有効/無効切替・編集・削除を行う画面 |
| 通知項目 | タイトル、通知開始時間、通知終了時間、表示文字を持つデータ |
| 通知対象 | 有効化されており、指定時刻になったら通知表示される項目 |
| 削除モード | 登録通知一覧画面で、削除対象を選択するための状態 |
| 時間軸 | 通知時刻を現実時間として扱うか、エオルゼア時間として扱うかを示す設定 |
| 現実時間 | Windows PCの現在時刻を基準にした通常の時刻 |
| エオルゼア時間（ET） | FF14のゲーム内時間。現実時間より高速に進む時間軸 |
| ピンON | 通常画面を常に最前面表示にする状態 |
| ピンOFF | 通常画面を通常のウィンドウと同じ重なり順で扱う状態 |

---

## 5. 画面一覧

| 画面名 | 表示タイミング | 主な役割 |
|---|---|---|
| 通常画面 | アプリ起動時 | 常駐タブ表示、通知表示、設定画面起動、最前面切替 |
| 設定画面 | 通常画面の歯車ボタン押下時 | 通知項目の新規登録・編集 |
| 登録通知一覧画面 | 設定画面の登録通知一覧ボタン押下時 | 登録済み通知の確認、有効/無効切替、編集、削除 |

---

## 6. 通常画面仕様

### 6.1 表示仕様

通常画面は、タブだけのような小さなウィンドウとして表示する。

- 起動時は画面右上付近に表示する
- 通常のウィンドウと同様にドラッグ移動できる
- ウィンドウ枠は通常のデスクトップアプリよりも控えめな見た目にする
- 常駐していても邪魔になりにくいサイズにする
- 表示位置はユーザーが移動できる
- アプリ終了後、次回起動時に前回位置を復元できることが望ましい

### 6.2 通常画面の表示要素

通常画面には以下を表示する。

| 要素 | 説明 |
|---|---|
| ウィンドウ名 | アプリ名または任意の固定名称を表示する |
| ピンマーク | 常に最前面表示のON/OFFを切り替える |
| 歯車マーク | 設定画面を開く |
| 通知表示エリア | 通知時に表示文字を表示する領域 |

表示順は以下を基本とする。

```text
[ウィンドウ名] [ピン] [歯車]
```

歯車マークの左隣にピンマークを配置する。

### 6.3 半透明表示

通常画面は普段は半透明で描画する。

- マウスが乗っていない状態:
  - 半透明で表示する
  - 背景が透けて見える程度にする
- マウスオーバー状態:
  - 通常時より若干濃く表示する
  - ただし完全不透明にはしない
  - 明度100%のような強い表示にはしない

推奨値:

| 状態 | 不透明度の目安 |
|---|---:|
| 通常時 | 40%〜60% |
| マウスオーバー時 | 70%〜85% |

### 6.4 常に最前面表示

通常画面には常に最前面表示の切替機能を持たせる。

- ピンON:
  - 通常画面を常に最前面で描画する
- ピンOFF:
  - 通常のウィンドウと同じ重なり順にする

初期状態はピンONとする。  
これにより、起動直後は通常画面が常に最前面で表示される。

### 6.5 最小化

通常画面は最小化できるようにする。

- 最小化した場合、通常画面は非表示になる
- 最小化中もアプリ自体は終了しない
- 最小化中も通知時刻の監視は継続する
- 最小化中に通知が発火した場合の挙動は以下とする
  - 通知表示のために通常画面を復帰させる
  - またはOS標準通知を併用する

初期実装では「通知発火時に通常画面を復帰させる」を優先する。

---

## 7. 通知表示仕様

### 7.1 通知発火条件

登録済み通知項目のうち、以下の条件を満たすものを通知対象とする。

- 通知対象チェックがONである
- 通知項目に設定された時間軸において、現在時刻が通知開始時間に到達した
- 同じ発火単位内で、まだ通知していない

時間軸ごとの扱いは以下とする。

| 時間軸 | 判定に使う現在時刻 | 繰り返し単位 |
|---|---|---|
| 現実時間 | PCの現在時刻 | 1日ごと |
| エオルゼア時間（ET） | PCの現在時刻から算出したET | ET 1日ごと |

現実時間の場合は、従来通り毎日同じ現実時刻に通知する。  
エオルゼア時間の場合は、現実時間ではなくETの同じ時刻帯になったタイミングで通知する。ET 1日は現実時間70分で進行するため、同じET時刻は現実時間で約70分ごとに繰り返し到来する。

日付指定、曜日指定、単発通知は初期実装の対象外とする。

### 7.2 通知時の動作

通知開始時間になったら、以下を同時に実行する。

- 通常画面付近に表示文字を表示する
- 通知音を再生する

通知表示は通常画面のウィンドウ付近に表示する。  
別の大きな管理画面を表示するのではなく、通常画面に紐づいた小さな通知表示として扱う。

### 7.3 通知表示内容

通知には以下を表示する。

- タイトル
- 表示文字
- 閉じるボタン

表示例:

```text
[タイトル]                         [×]
表示文字
```

### 7.4 通知終了時間

通知終了時間は、通知表示を自動的に消す時刻として扱う。

- 通知開始時間になったら通知を表示する
- 通知終了時間になったら通知表示を自動で閉じる
- ユーザーが×ボタンで閉じた場合は、その通知表示を即時閉じる
- ×で閉じた通知は、同じ発火単位内の通知終了時間まで再表示しない

### 7.5 複数通知が同時刻に発火した場合

同時刻に複数の通知項目が発火した場合は、通常画面付近に複数件表示する。

表示方法は以下のいずれかとする。

- 縦に並べて表示する
- 1つの通知領域内に複数件をリスト表示する

初期実装では「縦に並べて表示」を優先する。

### 7.6 通知音

通知発火時に通知音を鳴らす。

初期実装では以下のいずれかでよい。

- OS標準の通知音
- アプリ同梱の短い通知音

通知音の種類変更は初期実装の対象外とする。

### 7.7 時間軸選択

通知項目ごとに、通知判定に使う時間軸を選択できる。

- 現実時間
- エオルゼア時間（ET）

設定画面では「エオルゼア時間」チェックボックスを表示する。

| チェック状態 | 扱い |
|---|---|
| OFF | 通知開始時間・通知終了時間を現実時間として扱う |
| ON | 通知開始時間・通知終了時間をエオルゼア時間として扱う |

エオルゼア時間チェックがONの場合、入力欄に入力されたHH:mmはETのHH:mmとして扱う。  
現実時間のHH:mmをETへ変換して入力するのではなく、ユーザーが直接ETの時刻を入力する。

例:

```text
エオルゼア時間: ON
通知開始時間: 08:00
通知終了時間: 09:00
```

この場合、現実時間の08:00〜09:00ではなく、現在のETが08:00〜09:00の範囲に入ったときに通知する。

---

## 8. 設定画面仕様

### 8.1 表示仕様

設定画面は、通常画面の歯車マークを押すことで開く。

- 通常画面とは別ウィンドウとして起動する
- 半透明ではなく通常の不透明ウィンドウとして描画する
- 通常のデスクトップアプリの設定画面として扱う
- 複数起動は避け、既に開いている場合は既存ウィンドウを前面に出す

### 8.2 設定画面の入力項目

設定画面では、主に以下を設定できる。

| 項目 | 入力形式 | 必須 | 説明 |
|---|---|---:|---|
| タイトル | テキストボックス | 必須 | 通知のタイトル |
| エオルゼア時間 | チェックボックス | 任意 | ONの場合、通知開始時間・通知終了時間をエオルゼア時間として扱う |
| 通知開始時間 | 時刻入力 | 必須 | 通知を開始する時刻。エオルゼア時間チェックON時はETとして入力する |
| 通知終了時間 | 時刻入力 | 必須 | 通知を自動で閉じる時刻。エオルゼア時間チェックON時はETとして入力する |
| 表示文字 | 複数行テキストボックス | 必須 | 通知時に表示する本文 |

### 8.3 エオルゼア時間チェックボックス

設定画面には「エオルゼア時間」チェックボックスを実装する。

- チェックOFFの場合、通知開始時間・通知終了時間は現実時間として扱う
- チェックONの場合、通知開始時間・通知終了時間はエオルゼア時間として扱う
- チェックONの場合でも、入力形式はHH:mmとする
- チェックONの場合、ユーザーはエオルゼア時間のHH:mmを直接入力する
- チェックのON/OFFを切り替えても、入力済みの時刻文字列は自動変換しない
- チェックのON/OFFは通知項目ごとに保存する

例:

```text
エオルゼア時間: ON
通知開始時間: 12:00
通知終了時間: 12:30
```

この場合、12:00〜12:30は現実時間ではなくET 12:00〜12:30として判定する。

### 8.4 通知終了時間の自動入力

通知開始時間を入力したら、通知終了時間に「通知開始時間の1分後」を自動入力する。  
エオルゼア時間チェックがONの場合、この「1分後」はエオルゼア時間上の1分後として扱う。

例:

| 通知開始時間 | 自動入力される通知終了時間 |
|---|---|
| 09:00 | 09:01 |
| 12:34 | 12:35 |
| 23:59 | 00:00 |

ただし、通知終了時間は手動変更できる。  
ユーザーが通知終了時間を手動変更した後に通知開始時間を変更した場合は、以下のどちらかの仕様とする。

- 通知開始時間変更時に再度1分後を自動入力する
- 手動変更済みの場合は通知終了時間を維持する

初期実装では「通知開始時間変更時に再度1分後を自動入力する」を採用する。

### 8.5 ボタン

設定画面には以下のボタンを配置する。

| ボタン | 説明 |
|---|---|
| 保存 | 入力内容を通知項目として保存する |
| キャンセル | 入力内容を破棄して画面を閉じる |
| 登録通知一覧 | 登録通知一覧画面を開く |

### 8.6 新規登録時の動作

通常画面の歯車マークから設定画面を開いた場合は、新規登録モードとして扱う。

- 入力項目は空欄で表示する
- エオルゼア時間チェックの初期値はOFFとする
- 通知開始時間が入力されたら通知終了時間を自動入力する
- 保存ボタン押下で新しい通知項目を作成する

### 8.7 編集時の動作

登録通知一覧画面から既存項目をクリックした場合は、編集モードとして設定画面を開く。

- 選択した通知項目の内容を各入力欄に表示する
- 保存ボタン押下で既存通知項目を更新する
- 新しい通知項目として重複登録しない

---

## 9. 登録通知一覧画面仕様

### 9.1 表示仕様

登録通知一覧画面は、設定画面の「登録通知一覧」ボタンを押すことで開く。

- 設定画面と同様、半透明ではなく通常表示する
- 登録済みのすべての通知項目をリスト表示する
- 有効/無効状態を一覧から切り替えられる
- 各項目をクリックすると編集できる
- 削除モードに切り替えられる

### 9.2 通常モードの一覧表示

通常モードでは、各通知項目を以下のように表示する。

```text
[通知対象チェック]  タイトル  時間軸  通知開始時間 - 通知終了時間
                   表示文字の先頭数十文字
```

表示項目:

| 項目 | 説明 |
|---|---|
| 通知対象チェック | ONの場合は通知対象、OFFの場合は通知対象外 |
| タイトル | 通知タイトル |
| 時間軸 | 現実時間またはETであることを表示する |
| 通知開始時間 | 通知を開始する時刻 |
| 通知終了時間 | 通知を自動で閉じる時刻 |
| 表示文字プレビュー | 表示文字の一部 |

### 9.3 通知対象チェック

各項目の左側に通知対象チェックを表示する。

- チェックON: 通知対象に含める
- チェックOFF: 通知対象から除外する

チェックOFFの項目は、通知開始時間になっても通知しない。

### 9.4 項目クリックによる編集

登録通知一覧画面の各項目をクリックすると、設定画面と同じ形式で編集できる。

- 通知対象チェック部分をクリックした場合は、有効/無効切替のみ行う
- チェック以外の項目領域をクリックした場合は、編集画面を開く
- 編集画面ではタイトル、通知開始時間、通知終了時間、表示文字を変更できる

### 9.5 削除ボタン

登録通知一覧画面の右上付近に削除ボタンを配置する。

通常モードで削除ボタンを押すと、削除モードに切り替わる。

---

## 10. 登録通知一覧画面 - 削除モード仕様

### 10.1 削除モードへの切替

登録通知一覧画面の削除ボタンを押すと、削除モードになる。

削除モードでは、通常モードとは異なる削除対象選択用のチェックボックスを表示する。

### 10.2 削除モード時の表示

削除モードでは、通常の通知対象チェックを非表示にし、削除対象選択用チェックボックスを表示する。

```text
[削除対象チェック]  タイトル  時間軸  通知開始時間 - 通知終了時間
                   表示文字の先頭数十文字
```

### 10.3 チェックボックスの見た目

削除対象チェックボックスは、通知対象チェックと区別しやすい見た目にする。

例:

- 通知対象チェック: 通常のチェックマーク
- 削除対象チェック: 赤系のチェックボックス、ゴミ箱アイコン付き、または削除用であることが分かる枠線

### 10.4 削除実行

削除モードで削除対象にチェックを付けた状態で削除ボタンを押すと、削除確認ダイアログを表示する。

削除確認ダイアログには以下を表示する。

- 「削除しますか？」というメッセージ
- 削除対象となる項目のタイトル一覧
- 削除するボタン
- キャンセルボタン

表示例:

```text
削除しますか？

削除対象:
- 朝の確認
- 昼休み終了
- 作業終了前チェック

[削除する] [キャンセル]
```

### 10.5 削除するボタン

削除確認ダイアログで「削除する」を押した場合、選択された通知項目を削除する。

- 削除後、登録通知一覧画面を更新する
- 削除された項目は保存データからも削除する
- 削除後は削除モードを終了して通常モードに戻る

### 10.6 キャンセルボタン

削除確認ダイアログで「キャンセル」を押した場合、削除を行わない。

- 通知項目は削除しない
- 登録通知一覧画面に戻る
- 削除モードは維持しても終了してもよい

初期実装では「削除モードを維持する」を採用する。

### 10.7 削除対象未選択時

削除モードで削除対象が1件も選択されていない状態で削除ボタンを押した場合は、削除確認ダイアログを表示しない。

代わりに、以下のようなメッセージを表示する。

```text
削除する項目を選択してください。
```

---

## 11. データ仕様

### 11.1 通知項目データ

通知項目は以下のデータを持つ。

| フィールド名 | 型 | 必須 | 説明 |
|---|---|---:|---|
| id | string | 必須 | 通知項目の一意ID |
| title | string | 必須 | 通知タイトル |
| timeMode | string | 必須 | 通知時間の扱い。`real` または `eorzea` |
| startTime | string | 必須 | 通知開始時間。HH:mm形式 |
| endTime | string | 必須 | 通知終了時間。HH:mm形式 |
| message | string | 必須 | 表示文字 |
| enabled | boolean | 必須 | 通知対象かどうか |
| createdAt | string | 必須 | 作成日時。ISO 8601形式 |
| updatedAt | string | 必須 | 更新日時。ISO 8601形式 |

### 11.2 JSON保存例

```json
{
  "settings": {
    "mainWindow": {
      "x": 1400,
      "y": 80,
      "width": 260,
      "height": 48,
      "pinned": true,
      "opacityNormal": 0.5,
      "opacityHover": 0.8
    }
  },
  "notifications": [
    {
      "id": "b3b6c4e0-0001-0001-0001-000000000001",
      "title": "作業開始",
      "timeMode": "real",
      "startTime": "09:00",
      "endTime": "09:01",
      "message": "今日の作業内容を確認してください。",
      "enabled": true,
      "createdAt": "2026-05-17T09:00:00+09:00",
      "updatedAt": "2026-05-17T09:00:00+09:00"
    },
    {
      "id": "b3b6c4e0-0001-0001-0001-000000000002",
      "title": "未知採集",
      "timeMode": "eorzea",
      "startTime": "08:00",
      "endTime": "09:00",
      "message": "ET 08:00の採集ポイントを確認してください。",
      "enabled": true,
      "createdAt": "2026-05-17T09:00:00+09:00",
      "updatedAt": "2026-05-17T09:00:00+09:00"
    }
  ]
}
```

### 11.3 保存場所

保存場所はアプリのローカルデータ領域とする。

推奨例:

```text
%LOCALAPPDATA%\NotificationTabApp\settings.json
```

---

## 12. バリデーション仕様

### 12.1 タイトル

- 必須入力
- 前後の空白は保存時に除去する
- 空文字の場合は保存不可

### 12.2 時間軸

- エオルゼア時間チェックOFFの場合、`timeMode` は `real` として保存する
- エオルゼア時間チェックONの場合、`timeMode` は `eorzea` として保存する
- `timeMode` が `eorzea` の場合、通知開始時間・通知終了時間は現実時間ではなくETとして扱う
- `timeMode` が不明な値の場合は保存不可、または既存データ読み込み時に `real` として補正する

### 12.3 通知開始時間

- 必須入力
- HH:mm形式で保存する
- 無効な時刻は保存不可
- エオルゼア時間チェックONの場合も、入力値はHH:mm形式のETとして保存する

### 12.4 通知終了時間

- 必須入力
- HH:mm形式で保存する
- 無効な時刻は保存不可
- エオルゼア時間チェックONの場合も、入力値はHH:mm形式のETとして保存する
- 通知開始時間と同一でも保存不可とする

### 12.5 表示文字

- 必須入力
- 複数行入力可
- 前後の空白のみで構成される場合は保存不可

### 12.6 日跨ぎ

通知開始時間が23:59、通知終了時間が00:00のように日付を跨ぐケースは許可する。エオルゼア時間の場合も、ET上で日跨ぎする時間帯として扱う。

判定例:

| 開始 | 終了 | 扱い |
|---|---|---|
| 09:00 | 09:01 | 同日内通知 |
| 23:59 | 00:00 | 日跨ぎ通知 |
| 23:50 | 00:10 | 日跨ぎ通知 |

---

## 13. 通知スケジューリング仕様

### 13.1 時刻監視

アプリ起動中は、一定間隔で現在時刻を確認し、通知対象の通知開始時間に到達したか判定する。

- 監視間隔の目安: 1秒〜30秒
- 初期実装では1秒または10秒間隔を推奨
- 通知項目ごとに `timeMode` を確認し、現実時間またはエオルゼア時間のどちらで判定するかを切り替える

### 13.2 エオルゼア時間の算出

エオルゼア時間は、PCの現在時刻からアプリ内で算出する。FF14の画面、ログ、ゲームプロセスを読み取る必要はない。

換算仕様は以下とする。

| 項目 | 値 |
|---|---:|
| ET 1日 | 現実時間70分 |
| ET 1時間 | 現実時間2分55秒 |
| ET 1分 | 現実時間約2.916秒 |
| ET進行倍率 | 現実時間の約20.571428倍 |

計算方針:

```text
ET進行倍率 = 1440 / 70
現在Unix秒 = 現在UTC時刻のUnix秒
ET合計秒 = 現在Unix秒 * ET進行倍率
ET日内秒 = floor(ET合計秒) % 86400
ET時刻 = ET日内秒から HH:mm:ss を算出
```

C#実装例:

```csharp
public static TimeOnly GetEorzeaTime(DateTimeOffset nowUtc)
{
    const double eorzeaMultiplier = 1440.0 / 70.0;

    double earthUnixSeconds = nowUtc.ToUnixTimeSeconds();
    double eorzeaTotalSeconds = earthUnixSeconds * eorzeaMultiplier;

    int secondsInDay = (int)(Math.Floor(eorzeaTotalSeconds) % 86400);
    if (secondsInDay < 0)
    {
        secondsInDay += 86400;
    }

    int hour = secondsInDay / 3600;
    int minute = (secondsInDay % 3600) / 60;
    int second = secondsInDay % 60;

    return new TimeOnly(hour, minute, second);
}
```

### 13.3 時間範囲判定

通知開始時間・通知終了時間はHH:mm形式で保存する。判定時には、時間軸に応じて現在時刻を切り替える。

| timeMode | 現在時刻として使う値 | 入力値の意味 |
|---|---|---|
| `real` | PCの現在時刻 | 現実時間のHH:mm |
| `eorzea` | 算出した現在ET | エオルゼア時間のHH:mm |

日跨ぎの判定は、現実時間・エオルゼア時間のどちらでも同じロジックで扱う。

例:

| 開始 | 終了 | 現在 | 判定 |
|---|---|---|---|
| 08:00 | 09:00 | 08:30 | 通知期間内 |
| 08:00 | 09:00 | 09:30 | 通知期間外 |
| 23:50 | 00:10 | 23:55 | 通知期間内 |
| 23:50 | 00:10 | 00:05 | 通知期間内 |

### 13.4 通知済み管理

同じ通知が同じ発火単位内で複数回発火しないように、通知済み状態を管理する。

現実時間の場合は、現実の日付単位で管理する。

```json
{
  "notificationId": "b3b6c4e0-0001-0001-0001-000000000001",
  "lastTriggeredKey": "real:2026-05-17"
}
```

エオルゼア時間の場合は、ET日単位で管理する。ET日は現実時間70分ごとに進むため、現実の日付ではなくET日番号を使う。

```text
ET日番号 = floor(ET合計秒 / 86400)
```

保存例:

```json
{
  "notificationId": "b3b6c4e0-0001-0001-0001-000000000002",
  "lastTriggeredKey": "et:123456789"
}
```

### 13.5 アプリ起動時に既に通知時間内の場合

アプリ起動時点で、現在時刻が通知開始時間を過ぎており、通知終了時間前の場合は通知を表示する。

現実時間の例:

- 通知開始時間: 09:00
- 通知終了時間: 09:10
- アプリ起動時刻: 09:05

この場合、通知期間内のため通知を表示する。

エオルゼア時間の例:

- エオルゼア時間: ON
- 通知開始時間: 08:00
- 通知終了時間: 09:00
- アプリ起動時の現在ET: 08:30

この場合、ET上の通知期間内のため通知を表示する。

ただし、同じ発火単位内で既に通知済みの場合は再表示しない。通知終了時間を過ぎている場合は表示しない。

## 14. ウィンドウ状態保存仕様

通常画面について、以下の状態を保存できることが望ましい。

| 項目 | 説明 |
|---|---|
| x | ウィンドウ左上のX座標 |
| y | ウィンドウ左上のY座標 |
| width | ウィンドウ幅 |
| height | ウィンドウ高さ |
| pinned | ピンON/OFF状態 |
| opacityNormal | 通常時の不透明度 |
| opacityHover | マウスオーバー時の不透明度 |

初期実装では、最低限以下を保存する。

- x
- y
- pinned

---

## 15. エラー処理

### 15.1 設定ファイルが存在しない場合

設定ファイルが存在しない場合は、初期設定で起動する。

- 通常画面を画面右上付近に表示
- ピンON
- 通知項目なし

### 15.2 設定ファイルが壊れている場合

設定ファイルの読み込みに失敗した場合は、以下の対応を行う。

- 壊れた設定ファイルをバックアップする
- 初期設定で起動する
- ユーザーに設定読み込み失敗を通知する

バックアップ例:

```text
settings.json.bak
```

### 15.3 通知音の再生に失敗した場合

通知音の再生に失敗しても、通知表示は行う。

- アプリを異常終了させない
- 必要に応じてログに記録する

---

## 16. 非機能要件

### 16.1 起動

- Windows上で通常のアプリとして起動できること
- 起動時に通常画面を表示すること

### 16.2 操作性

- 通常画面はドラッグで移動できること
- ピンON/OFFが直感的に分かること
- 通知対象チェックと削除対象チェックの見た目が区別できること

### 16.3 視認性

- 通常画面は半透明でも文字が読めること
- マウスオーバー時は少し濃くなり、操作しやすいこと
- 通知表示は通常画面付近に出ていることが分かること

### 16.4 安定性

- 通知監視中にアプリが固まらないこと
- 設定画面を開閉しても通知監視が継続すること
- 通知項目の追加・編集・削除後、保存データと画面表示が一致すること

---

## 17. 受け入れ条件

### 17.1 起動・通常画面

- アプリ起動時に通常画面が表示される
- 通常画面が画面右上付近に表示される
- 通常画面をドラッグ移動できる
- 通常画面が半透明で表示される
- マウスオーバーで通常時より濃く表示される
- ピンON時に常に最前面表示になる
- ピンOFF時に通常ウィンドウと同じ重なり順になる
- 歯車ボタンから設定画面を開ける

### 17.2 設定画面

- 設定画面が別ウィンドウで開く
- 設定画面は半透明ではない
- タイトル、エオルゼア時間チェック、通知開始時間、通知終了時間、表示文字を入力できる
- エオルゼア時間チェックON時は、通知開始時間・通知終了時間がETとして扱われる
- 通知開始時間を入力すると通知終了時間に1分後が自動入力される
- 保存すると通知項目が登録される
- 登録通知一覧ボタンから登録通知一覧画面を開ける

### 17.3 通知

- 登録した通知開始時間になると通知が表示される
- エオルゼア時間チェックONの通知項目は、ETの通知開始時間になったときに通知が表示される
- 通知と同時に通知音が鳴る
- 通知にはタイトルと表示文字が表示される
- 通知は通常画面付近に表示される
- 通知は×ボタンで閉じられる
- 通知終了時間になると通知が自動で閉じる
- 通知対象チェックがOFFの項目は通知されない

### 17.4 登録通知一覧画面

- 登録済み通知がすべて一覧表示される
- 各項目の左側に通知対象チェックが表示される
- 通知対象チェックを外すと通知対象外になる
- 項目をクリックすると編集できる
- 編集内容を保存すると一覧と保存データに反映される

### 17.5 削除モード

- 削除ボタンで削除モードに切り替わる
- 削除モードでは通知対象チェックが消える
- 削除対象選択用の別チェックボックスが表示される
- 削除対象チェックは通知対象チェックと見た目が区別できる
- 削除対象を選んで削除ボタンを押すと確認ダイアログが表示される
- 確認ダイアログに「削除しますか？」と対象タイトル一覧が表示される
- 「削除する」で対象項目が削除される
- 「キャンセル」で削除されない

---

## 18. 初期実装で対象外とする機能

以下は初期実装では対象外とする。

- 曜日指定通知
- 日付指定通知
- 単発通知
- 通知音の種類選択
- 通知音の音量設定
- OS起動時の自動起動
- クラウド同期
- 複数端末同期
- ユーザーアカウント管理
- テーマ切替
- 詳細な通知履歴画面

---

## 19. 今後追加できる拡張案

将来的に以下を追加できる設計にしておくと望ましい。

- 曜日指定
- 日付指定
- 繰り返し周期設定
- 通知音選択
- スヌーズ
- 通知履歴
- ET通知の次回現実時刻表示
- Windows起動時に自動起動
- タスクトレイ常駐
- 設定データのエクスポート/インポート
- ダークモード/ライトモード

---

## 20. 実装時の注意点

- 通常画面の「常に最前面」とピンON/OFFは矛盾しないように実装する
  - 初期状態はピンON
  - ピンOFFにすると通常ウィンドウ扱いにする
- 設定画面と登録通知一覧画面は半透明にしない
- 削除モードのチェックボックスは、通知対象チェックと誤認されないデザインにする
- 通知開始時間入力時に通知終了時間を自動入力するが、通知終了時間は手動編集可能にする
- エオルゼア時間チェックON時は、通知開始時間・通知終了時間をETとして入力・保存・判定する
- エオルゼア時間チェックのON/OFF切替時に、入力済み時刻を勝手に現実時間/ETへ変換しない
- ET判定はFF14のプロセスや画面読み取りではなく、PCの現在時刻から計算する
- 設定画面は新規登録モードと編集モードを明確に分ける
- 通知項目のIDを使って編集・削除対象を特定する
- 通知項目のタイトルが重複しても、IDで別項目として扱う
- 設定ファイル破損時にアプリが起動不能にならないようにする

---

## 21. 画面遷移

```text
[アプリ起動]
    ↓
[通常画面]
    ├─ 歯車押下 → [設定画面]
    │                  ├─ 保存 → 通知項目登録/更新
    │                  ├─ キャンセル → 閉じる
    │                  └─ 登録通知一覧押下 → [登録通知一覧画面]
    │                                           ├─ 項目クリック → [設定画面: 編集モード]
    │                                           ├─ 通知対象チェックON/OFF
    │                                           └─ 削除ボタン → [削除モード]
    │                                                              ├─ 削除ボタン → [削除確認]
    │                                                              │                  ├─ 削除する → 削除実行
    │                                                              │                  └─ キャンセル → 削除しない
    │                                                              └─ キャンセル/戻る → 通常モード
    └─ ピン押下 → 常に最前面ON/OFF
```

---

## 22. 実装エージェントへの指示

この仕様をもとに、Windows向けデスクトップアプリを実装すること。

優先順位は以下とする。

1. 通常画面の表示、半透明、ドラッグ移動、ピンON/OFF
2. 設定画面による通知項目の登録
3. エオルゼア時間チェックとET時刻判定
4. 通知時刻の監視、通知表示、通知音再生
5. 登録通知一覧画面の表示、有効/無効切替
6. 既存通知項目の編集
7. 削除モードと削除確認ダイアログ
8. 設定ファイル保存、ウィンドウ位置復元

実装時に仕様上の不足がある場合は、勝手に大きく仕様変更せず、最小限の自然な補完に留めること。
